#!/usr/bin/env bash
set -euo pipefail

export PROJ_HOME="$(cd "$(dirname "$0")/.." && pwd)"
source "${PROJ_HOME}/bin/aux/test.sh"

tests="$(find "${PROJ_HOME}/bin" -iname "${1:-}*_test.sh")"

for test in $tests; do
   source "$test"
done

test::finish
